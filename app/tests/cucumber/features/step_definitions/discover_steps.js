(function() {
    'use strict';

    var assert = require('assert');

    module.exports = function() {

        /*
        this.Then(/^I should see some partup tiles$/, function(expectedUsername, callback) {
            this.browser.
            waitForExist('.pu-partuptile').
            getText('.pu-partuptile').should.eventually.contain('Benefiet');
        });

        this.When(/^I enter a discover keyword$/, function(expectedUsername, callback) {
            this.browser.
            waitForExist('[name="search_query"]').
            setValue('[name="search_query"]', 'Benefiet').
            submit('#discoverQuery').
            waitForExist('.pu-partuptile').
            getText('.pu-partuptile').
            then(function(tiles) {
                assert(tiles.length, 1);
                callback();
            });
        });
        */

    };

})();
